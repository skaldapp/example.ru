---
title: Главная Страница
meta:
  - name: description
    content: Пример простого сайта на skald
attrs:
  un-cloak: true
  class:
    - max-w-none
    - prose
    - prose-stone
    - text-center
script:
  - type: importmap
    innerHTML: |
      {
        "imports": {
          "element-plus": "https://cdn.jsdelivr.net/npm/element-plus@2/dist/index.full.min.mjs",
          "speech-code": "https://cdn.jsdelivr.net/npm/speech-code@2/index.js"
        }
      }
template: true
features:
    - name: Самый простой сайт
      text: Мы делаем самый простой сайт на skald
    - name: Самый быстрый сайт
      text: Мы делаем сайт на skald очень быстро
    - name: Самый технологичный сайт
      text: Мы делаем сайт, используя самые современные технологии
icon: twemoji:house
---

<script setup>
import { ElCard, ElMenu, ElMenuItem } from "element-plus";
import { inject } from "vue";
import { getParagraph } from "speech-code";
const text = Array(12).fill().map(() => getParagraph());
const docs = inject("docs");
</script>

<style src="https://cdn.jsdelivr.net/npm/element-plus@2/dist/index.css" />
<style lang="postcss">
.el-page-header {
  @apply "sticky top-0 pa-4 border-b border-slate-200 bg-neutral-50";
}

.el-menu--horizontal {
  @apply "justify-center";
}
</style>

<el-menu :default-active="$route.path" mode="horizontal" :router="true">
<el-menu-item :index="docs[$id].to"><div :class="`i-${$frontmatter.icon}`" class="size-5 mr-2" />{{ $frontmatter.title }}</el-menu-item>
<el-menu-item v-for="({ to, frontmatter: { title, icon } }, key) in docs[$id].$children" :key :index="to"><div :class="'i-' + icon" class="size-5 mr-2" />{{ title }}</el-menu-item>
</el-menu>

<div class="container mx-auto min-h-dvh my-24 px-4" un-cloak><router-view v-slot="{ Component }">
<component :is="Component" v-if="Component" />
<template v-else>

<el-card>
<div :class="`i-${$frontmatter.icon}`" class="size-12 my-4" />

# {{ $frontmatter.title }}

## {{ $frontmatter.meta[0].content }}

</el-card>

<br />

<br />

### Чем мы заняты

Мы делаем первый шаг к созданию сайта на skald

<br />

<br />

<div class="grid grid-cols-4 gap-6 gap-y-12 lg:grid-cols-12">
<div class="col-span-4" v-for="({ name, text }, index) in $frontmatter.features">
<div :class="'i-mdi:numeric-' + (Number(index) + 1) + '-circle-outline'" class="size-6" />

### {{ name }}

{{ text }}

</div>
</div>

<br />

<br />

### Основной текст

Текст был сгенерирован случайным образом

<br />

<p v-for="paragraph in text" class="text-justify">{{ paragraph }}</p>
</template>
</router-view>
</div>

<br />

<div class="bg-neutral-500 mt-auto prose prose-invert max-w-none" un-cloak>

<br />

**{{ $frontmatter.meta[0].content }}**

<br />

</div>
