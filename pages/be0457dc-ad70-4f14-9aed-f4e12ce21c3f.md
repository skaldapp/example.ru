---
title: Быстрый старт
meta:
  - name: description
    content: Быстрый старт в skald за 8 шагов
attrs:
  class:
    - min-h-dvh
    - flex
    - flex-col
    - max-w-none
    - prose
    - prose-stone
  un-cloak: true
script:
  - type: importmap
    innerHTML: |
      {
        "imports": {
          "element-plus": "https://cdn.jsdelivr.net/npm/element-plus@2/dist/index.full.min.mjs",
          "@element-plus/icons-vue": "https://cdn.jsdelivr.net/npm/@element-plus/icons-vue@2/dist/index.min.js",
          "speech-code": "https://cdn.jsdelivr.net/npm/speech-code@2/index.js"
        }
      }
flat: true
---

<el-page-header :icon :content :title v-on:back="back" />
<router-view />
<div class="bg-neutral-500 pa-12 mt-auto text-center prose prose-invert max-w-none">
<el-button-group class="not-prose">
<el-button v-for="{ to, frontmatter } in $children" tag="router-link" :to>{{ frontmatter.title }}</el-button>
</el-button-group>
<br />
<br />
{{ description }}
</div>

<script setup>
import ElementPlus from "element-plus";
import { AlarmClock as icon } from "@element-plus/icons-vue";
import { inject, computed, getCurrentInstance } from "vue";
import { useRoute, useRouter } from "vue-router";
const { appContext: { app } } = getCurrentInstance();
const route = useRoute();
const router = useRouter();
const pages = inject("pages");
const { $children } = pages[$id];
const { title, meta: [{ content: description }] } = $frontmatter;
const content = computed(() => pages[route.name].frontmatter.title);
const back = () => { router.push("/") };
app.use(ElementPlus);
</script>

<style src="https://cdn.jsdelivr.net/npm/element-plus@2/dist/index.css" />
<style lang="postcss">
.el-page-header {
  @apply "sticky top-0 pa-4 border-b border-slate-200 bg-neutral-50";
}
</style>

